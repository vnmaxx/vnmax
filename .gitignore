node_modules
dist
dist-portal
.DS_Store
*.log
.env
.env.local
.vercel

# Web sub-project (React app)
web/node_modules
web/dist
web/.env
web/.env.local

# Credenciais — nunca versionar
serviceAccount.json
*serviceAccount*.json

# Conteudo sensivel — fica no Firebase/Supabase, nao no repo publico
vnmax-os/
src/data-internal.mjs
exports/
