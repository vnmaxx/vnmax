{
  "name": "vnmax-assistant",
  "version": "1.1.0",
  "description": "Assistente de IA da VNMAX (NVIDIA NIM) + captura de leads para o CRM (Firestore)",
  "type": "module",
  "private": true,
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  }
}
